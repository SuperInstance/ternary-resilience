[package]
name = "ternary-resilience"
version = "0.1.0"
edition = "2021"
description = "Network resilience analysis with ternary edge weights — connectivity, flow, cascading failures, and backup paths"

[dependencies]
